import { Text, View } from "react-native";

export default function Saved() {
  return (
    <View className="flex justify-center items-center w-full h-full">
      <Text className="text-center font-bold">Saved</Text>
    </View>
  );
}
